{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/plugin-architecture/manifest.schema.json",
  "title": "Plugin manifest",
  "description": "The declarative contract every plugin ships (system-design.md §3). The Loader reads this BEFORE any plugin code runs, so discovery, load order, version compatibility, namespacing and security are enforced from it. Cross-field rules a JSON Schema cannot express (e.g. every permission/event/provided token must be prefixed with `id` — §6) are validated by the Loader at boot; this schema enforces the structural shape.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "name", "version", "coreVersion"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Globally unique id — also the MANDATORY namespace prefix for every registered key (§6).",
      "pattern": "^[a-z][a-z0-9-]*$"
    },
    "name": { "type": "string", "minLength": 1 },
    "version": {
      "type": "string",
      "description": "The plugin's own semver.",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
    },
    "coreVersion": {
      "type": "string",
      "description": "Semver RANGE of the Core this plugin is compatible with (§4); the Loader refuses to load a plugin whose range excludes the running Core.",
      "minLength": 1
    },
    "kind": {
      "type": "string",
      "description": "Plugin kind (§3.1). `capability` = in-process module, same language as Core, talks via DI + Event Bus (the default). `tool` = an out-of-process backing service/datastore (Postgres, Redis, object store, a bot gateway) that ships its own container via `compose` and exposes only a namespaced connection contract. `app` = an out-of-process feature that owns its container AND its database and reaches Core only over HTTP + events — may be written in any language. A `tool`/`app` sits behind the network seam, so it MAY be polyglot; a `capability` MUST NOT.",
      "enum": ["capability", "tool", "app"],
      "default": "capability"
    },
    "dependencies": {
      "type": "array",
      "description": "Plugin ids that MUST boot first (§4). The Loader topologically sorts on these; a cycle is a hard boot failure.",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
      "uniqueItems": true,
      "default": []
    },
    "optionalDependencies": {
      "type": "array",
      "description": "Used if present, degraded-but-functional if absent.",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
      "uniqueItems": true,
      "default": []
    },
    "provides": {
      "type": "array",
      "description": "Namespaced DI tokens this plugin registers into the container (e.g. `inventory.StockService`).",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*\\.[A-Za-z][A-Za-z0-9]*$" },
      "uniqueItems": true,
      "default": []
    },
    "consumes": {
      "type": "array",
      "description": "Namespaced contracts this plugin resolves via DI. Every entry must be `provide`d by Core or by a declared dependency (Loader-checked).",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*\\.[A-Za-z][A-Za-z0-9]*$" },
      "uniqueItems": true,
      "default": []
    },
    "permissions": {
      "type": "array",
      "description": "Permission keys — all prefixed with `id` (§6), e.g. `crm.lead.edit`.",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*\\.[a-z0-9.-]+$" },
      "uniqueItems": true,
      "default": []
    },
    "routes": {
      "type": "array",
      "description": "Route prefixes this plugin owns — namespaced by id, e.g. `/crm/*`.",
      "items": { "type": "string", "pattern": "^/" },
      "uniqueItems": true,
      "default": []
    },
    "events": {
      "type": "object",
      "additionalProperties": false,
      "description": "Event names this plugin emits / listens to (Event Bus, §5).",
      "properties": {
        "emits": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*\\.[a-z0-9.-]+$" },
          "uniqueItems": true,
          "default": []
        },
        "listens": {
          "type": "array",
          "items": { "type": "string", "pattern": "^[a-z][a-z0-9-]*\\.[a-z0-9.-]+$" },
          "uniqueItems": true,
          "default": []
        }
      }
    },
    "config": {
      "type": "object",
      "additionalProperties": false,
      "description": "Validated config + feature flags (§11).",
      "properties": {
        "schema": { "type": "string", "description": "Path to this plugin's config JSON Schema." }
      }
    },
    "migrations": {
      "type": "string",
      "description": "Path to this plugin's migrations; run in dependency order (§4)."
    },
    "compose": {
      "type": "string",
      "description": "Path to a Docker Compose fragment this plugin contributes — required for `kind: tool`/`app`, which ship their own sidecar container(s). The App merges every enabled plugin's fragment into the generated stack; disable the plugin and its service disappears (§3.1)."
    },
    "secrets": {
      "type": "array",
      "description": "Names of the secret/config keys this plugin needs (e.g. `database_url`) — resolved from the environment/secret store at boot, never hardcoded. Lets the App validate that every required secret is present before it starts a `tool`/`app` sidecar.",
      "items": { "type": "string", "pattern": "^[a-z][a-z0-9_]*$" },
      "uniqueItems": true,
      "default": []
    }
  }
}
